package guichet.physique;import compte.Argent;import guichet.Guichet;import simulation.Simulation;/** Gestionnaire du panneau operateur du guichet. Dans un vrai guichet, ceci gere un composant physique;  * 	dans cette simulation, ca utilise les classes dans le package simulation. */ public class OperatorPanel{    /** Constructor     *     *  @param atm le guichet qui possede ce panneau operateur     */    public OperatorPanel(Guichet atm)    {        this.atm = atm;    }        // Dans un vrai guichet, du code serait necessaire pour detecter un changement     // d'etat du switch et notifier le guichet - ici on le simule par un boutton dans l'interface        /** Donne le montant d'argent dans le distributeur a sa mise en marche      *     *  @return valeur en dollars du montant dans le distributeur (# de billets x $20)     */        public Argent getInitialCash()    {        return Simulation.getInstance().getInitialCash();    }        /** Le guichet qui possede ce panneau operateur.      * L'objet guichet sera notifie si la switch du panneau devient ON ou OFF     */    private Guichet atm;    }